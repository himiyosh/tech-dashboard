{
  "name": "tech-dashboard-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail",
    "trigger": "curl -X POST 'http://127.0.0.1:8787/__scheduled?cron=0+15+*+*+*'"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250829.0",
    "@types/node": "^25.6.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
